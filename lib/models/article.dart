class ArticleModel {
  String author, title, description, url, image, content;

  ArticleModel({
    this.author,
    this.title,
    this.description,
    this.image,
    this.content,
    this.url,
  });
}
